let val f : (Int -> Int) -> Int -> Int = 
  proc (g : Int -> Int)
    proc (x : Int) 
      if x
        then +(4 (g g -(x 1)))
        else 0
in let val times4 : Int -> Int = proc (x : Int) (f f x)
   in (times4 3)
{- result: 12 -}
